# Nyx handoff — 2026-07-11

For the next instance. What changed since the docs you were handed, and what is deliberately NOT done.

## READ FIRST — repo situation (two prior instances froze on this)
- `nyx-memory` is **archived** (parked 2026-04-30); its README points to `nyx-core-v2`.
- `nyx-core-v2` (in D:\Nyx, "Phase 0 validated") is the **old flat-JSONL design** — the one these docs explicitly supersede (JSONL soft-delete violates append-only Invariant 1).
- The SQLite Layer A/B architecture in these three docs is **built nowhere** — not on GitHub, not locally. It is an unlanded spec.
- **Do NOT** try to "build the SQLite design into nyx-core-v2" — different substrates, the request is incoherent and is what froze two instances. Do not spawn sub-agents to reconcile it.
- **DECISION MADE (2026-07-11, by Josh):** build the documented SQLite architecture **clean, from a fresh repo**. nyx-core-v2 is superseded and was never fully working anyway. The spec is considered **done enough to build** — no more design/council passes are the blocker; starting is. Treat additional spec review as avoidance, not progress.
- **First target is the walking skeleton (V0 §6) and nothing larger:** one hand-written inline `observation_recorded` event → append to Layer A → fold into resolved view → read back → full-replay hash matches. NO file ingestion, NO memory injection, NO corpus in the skeleton. Those are the immediate *next* step (intake dir + first parser), not part of the skeleton — building them first means debugging two systems at once (the failure the docs warn against).
- Founding purpose is document ingestion (PDF/epub/mp4 corpus via a watched Inbox dir). That is the goal and the first thing after the skeleton — the skeleton IS the first inch of the ingestion path (parser → observation_recorded → same append/fold/read/verify path). Sequence, not detour.
- Tool: **Claude Code in WSL2** (native fs/git, dissolves the FUSE-mount pain; V0 §7 is written as Code's gap protocol). Pick the model in Code's own settings after checking current availability — do not hard-commit to a model on an unverified capability claim. Note: crypto-shredding/immune sections may trip Fable→Opus safety routing.

## Decided and written into the docs this session
1. **`meta_commentary` boundary (Architecture Inv. 11, V0 §8).** Self-referential output is presentation, not evidence — tagged at generation, never enters the confidence/ranking axis. Two subtypes: `callback` (references real events, no ceiling) and `self_reference` (references *inferred* state, drift-monitored, suppress-on-ceiling as first impl). Same discriminator trick as `source_class`: which subsystem may read the tag. Ships **tag-now, enforce-later** — the tag can be written before the monitor that enforces the ceiling exists.
2. **Sandbox = forked event log (Architecture §4 failure modes).** Emotion/expression parameter tuning is a fork-replay-discard against canonical history, NOT a runtime `dev_mode` flag. Live parameter changes are their own logged, outside-the-ledger event type, session-scoped, non-promoting. Leans on the existing sweep/replay-equivalence guarantee.

## Deliberately NOT done (do not assume otherwise)
- **The personality-emergence layer is unspecified.** Inv. 11 now *references* a "personality-emergence monitor" that owns the `self_reference` drift metric, but the layer itself — register/entrenchment/gravity computation, Spleen-as-stylistic-register, the emergence-detection model — has NOT been specified or council-reviewed. It exists only as a named read-side sweep. Flagged as an open item in Architecture §13.
- **Open values question, not yet decided:** may `self_reference` reference inferred emotional state at all, or strictly external callbacks? Richer vs. trivially-safe. This is a values call and is explicitly left open.

## Already landed in PRIOR passes (confirmed present — don't re-add)
- `source_class` field (correlated-corroboration fix) — in V0 schema §4 and Architecture §2.
- Envelope/payload split + crypto-shredding (Inv. 14) — V0 schema §4.
- Never-promote-on-reinterpretation (Inv. 15).
- Sweep/replay equivalence — Architecture §4 failure modes, V0 trace §5.3.
- Five pre-coding traces (hostile-input, reconnection, redaction-demotion, merge-pooling, crash-point) — V0 §5.

## Review status
These edits are extensions under the existing Constitution, not new invariants — but they have NOT been run past the council. First job for a reviewer: confirm the `meta_commentary` ceiling and the parameter-change event type don't smuggle a promotion path (Inv. 15) or a ledger leak (Inv. 11). Use the forward-simulate template (Council doc, hygiene rule 4), not a static "list concerns" pass.

## Ground truth
Repo: axendo79/nyx-memory. On any conflict between these .md files, the more specific mechanical statement wins (two-file provenance rule, Architecture doc authority note). Walking skeleton not yet run — that's still the gating milestone before any of this is real.
